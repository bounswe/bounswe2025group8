# Backend API Configuration
# API host address (e.g., localhost, 192.168.1.100, or production domain)
API_HOST=localhost

# API port (default: 8000)
API_PORT=8000

# Production API URL (uncomment and set for production builds)
PRODUCTION_API_URL=http://35.222.191.20:8000

# Android Build Configuration
# Build type: debug or release
BUILD_TYPE=debug

# Android Package Name (optional, defaults to expo slug)
# ANDROID_PACKAGE_NAME=com.yourcompany.mobile

# Android Version Code (auto-incremented if not set)
# ANDROID_VERSION_CODE=1

# Android Version Name (defaults to expo version)
# ANDROID_VERSION_NAME=1.0.0
