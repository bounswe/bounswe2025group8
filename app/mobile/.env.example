# =============================================================================
# Environment Variables for Neighborhood Assistance Mobile App
# =============================================================================
# Copy this file to .env and fill in the values for your environment.
# 
# IMPORTANT: Never commit .env files with real credentials to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------

# The host address of the backend API server
# - For Production: Use your production server IP (default)
# - For Android Emulator: Use 10.0.2.2 (special IP that routes to host machine)
# - For iOS Simulator: Use your computer's LAN IP (e.g., 192.168.1.100)
# - For Physical Device: Use your computer's LAN IP or production server IP
API_HOST=35.222.191.20

# The port number of the backend API server
# Default Django development server port is 8000
API_PORT=8000

# -----------------------------------------------------------------------------
# Build Configuration (used during Docker build)
# -----------------------------------------------------------------------------

# Build type: 'debug' or 'release'
# - debug: Faster builds, includes debugging tools, larger APK size
# - release: Optimized builds, smaller APK size, suitable for distribution
BUILD_TYPE=release
